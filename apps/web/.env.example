# Web Application Environment Configuration
# Copy this file to .env.dev or .env.prod for local deployment
# These files are git-ignored for security

# =============================================================================
# LOCAL DEVELOPMENT (.env.local)
# =============================================================================
# VITE_ENV=LOCAL
# VITE_PROJECT=FLOWS
# VITE_API_URL=http://localhost:8800
# VITE_WS_ENDPOINT=ws://localhost:8801
# VITE_CODES_URL=https://console-dev.eureka.codes

# =============================================================================
# TEMPLATE (fill in your values)
# =============================================================================
VITE_ENV=LOCAL
VITE_PROJECT=FLOWS
# VITE_PROCESS_API=real    # Enable real API for Process Navigator (default: mock)
VITE_API_URL=http://localhost:8800
VITE_WS_ENDPOINT=ws://localhost:8801
VITE_CODES_URL=https://console.eureka.codes

# Optional: opt the flow-editor Agent Panel into the real, credit-spending
# eureka-flows-api Generate gateway (text-only — it will not move nodes yet).
# Unset or any other value keeps the default offline command gateway.
# VITE_AGENT_GATEWAY=generate-sync
# Set to `generate-ws` instead for the WebSocket/SLS variant (same text-only scope).
# VITE_AGENT_GATEWAY=generate-ws

# Optional: S3 bucket URL for i18n translations.
# Must end with the stage prefix (dev/prod) — the app appends /{lng}/{ns}.json,
# and translation files live under s3://<bucket>/<stage>/<lng>/<ns>.json.
# VITE_I18N_BUCKET_URL=https://your-bucket.s3.ap-northeast-2.amazonaws.com/dev

# Optional: billing app URL. When set, a "Buy credits" button appears in the editor
# header and deep-links here. Leave unset (open-source self-host) to hide the button.
# VITE_BILLING_URL=https://billing.example.com

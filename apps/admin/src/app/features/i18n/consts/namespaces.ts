/** Must match the web app's i18n config (apps/web/src/i18n/index.ts) */
export const DEFAULT_NAMESPACES = ['common', 'flows', 'nodes', 'landing', 'tutorial', 'blocks'];
export const DEFAULT_LANGUAGES = ['en', 'ko'];
